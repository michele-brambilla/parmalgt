#include <Point.hpp>

const pt::Direction pt::Direction::t = pt::Direction(0);
const pt::Direction pt::Direction::x = pt::Direction(1);
const pt::Direction pt::Direction::y = pt::Direction(2);
const pt::Direction pt::Direction::z = pt::Direction(3);
